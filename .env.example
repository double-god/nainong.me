# ==========================================
# Docker Compose 环境变量模板
# ==========================================
# 使用方法: 复制为 .env 文件并填写实际值

# Docker Hub 配置 (生产环境)
DOCKER_USERNAME=your-dockerhub-username

# API 地址 (前端需要访问后端 API)
PUBLIC_API_URL=https://api.nainong.me

# 容器用户配置 (避免文件权限问题)
# 在服务器上运行 'id -u' 和 'id -g' 获取 UID 和 GID
# GitHub Actions Runner 通常使用 1000:1000
UID=1000
GID=1000

# 端口配置 (避免与 1Panel 等服务冲突)
# HTTP: 8080, HTTPS: 8443
# 如需使用 80/443 端口，请先停止 1Panel 或修改其端口

# SSH 部署配置 (仅用于 CI/CD，不要在本地使用)
# SSH_HOST=your-server-ip
# SSH_USER=root
# SSH_PORT=22
# DEPLOY_PATH=/root/my-blog
